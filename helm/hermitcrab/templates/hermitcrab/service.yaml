{{- if .Values.hermitcrab.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hermitcrab.name" . }}
  namespace: {{ include "hermitcrab.namespace" . }}
  {{- if .Values.commonAnnotations }}
  annotations:
    {{- toYaml .Values.commonAnnotations | nindent 4 }}
  {{- end }}
  labels:
    {{- include "hermitcrab.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.hermitcrab.service.type }}
  {{- if .Values.hermitcrab.service.sessionAffinity }}
  sessionAffinity: {{ .Values.hermitcrab.service.sessionAffinity }}
  {{- end }}
  {{- if .Values.hermitcrab.service.sessionAffinityConfig }}
  sessionAffinityConfig:
    {{- toYaml .Values.hermitcrab.service.sessionAffinityConfig | nindent 4 }}
  {{- end }}
  ports:
    - name: http
      port: {{ .Values.hermitcrab.service.ports.http }}
      targetPort: http
      protocol: TCP
    - name: https
      port: {{ .Values.hermitcrab.service.ports.https }}
      targetPort: https
      protocol: TCP
  selector:
    {{- include "hermitcrab.selectorLabels" . | nindent 4 }}
{{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hermitcrab.name" . }}-headless
  namespace: {{ include "hermitcrab.namespace" . }}
  {{- if .Values.commonAnnotations }}
  annotations:
    {{- toYaml .Values.commonAnnotations | nindent 4 }}
  {{- end }}
  labels:
    {{- include "hermitcrab.selectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: http
      port: {{ .Values.hermitcrab.service.ports.http }}
      targetPort: http
      protocol: TCP
    - name: https
      port: {{ .Values.hermitcrab.service.ports.https }}
      targetPort: https
      protocol: TCP
  selector:
    {{- include "hermitcrab.selectorLabels" . | nindent 4 }}
